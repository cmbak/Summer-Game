using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FinishFlag : MonoBehaviour
{

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //If level is {name of last level}
        //Then show Game completed screen etc.
        //Should you keep a total tally of coins - e.g. the amount continues onto the next level
    }
}
